package com.generated.rescueStock.constants;

/** 调拨单状态：草稿 / 已提交待审批 / 审批通过已预占 / 已出库确认实扣 / 已签收 / 已驳回 / 已取消 */
public enum DispatchStatus {
  DRAFT,
  SUBMITTED,
  APPROVED,
  DISPATCHED,
  RECEIVED,
  REJECTED,
  CANCELLED
}
