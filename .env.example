# Compose 项目名（容器名前缀）
COMPOSE_PROJECT_NAME=rescue-stock

# 前端宿主机访问端口
FRONTEND_PORT=20102

# 后端宿主机访问端口（容器内固定 8080）
BACKEND_PORT=21102

# MySQL 宿主机映射端口
DB_PORT=33060

# 数据库凭据
DB_NAME=app_db
DB_USER=app_user
DB_PASSWORD=app_password
